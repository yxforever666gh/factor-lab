# 2026-03-23

- Factor Lab now has a formal promotion/robustness layer: added a promotion scorecard, surfaced it in the latest summary and Exposure UI, and made the system classify candidates as core / validate / dedupe / regime-sensitive / drop instead of just showing raw scores.
- Added a robustness batch runner that auto-builds and runs top-candidate validation across `30d/45d/60d/90d/120d + expanding` windows. The key empirical result from these runs: recent winners mostly survive only in `30d/45d` and fail from `60d+`, so the strongest current signals are short-window and fragile rather than robust cross-window alpha.
- Added duplicate-aware promotion logic using `duplicate_of`, `refinement_of`, and high-correlation relationships. This stops the system from treating multiple near-identical hybrids as separate wins.
- Current deduped frontier focus for Factor Lab is centered on three candidates: `hybrid_liquidity_turnover_shock_mom_20`, `hybrid_mom_20_value_ep`, and `hybrid_size_small_value_bp`. Suppressed/off-frontier names currently include `mom_20`, `mom_plus_value`, `liquidity_turnover_shock`, `value_ep`, `value_bp`, `size_small`, `hybrid_mom_20_liquidity_turnover_shock`, and `hybrid_liquidity_turnover_shock_value_ep`.
- Wired frontier focus back into research planning so the candidate pool prefers deduped representatives instead of blindly following historical candidate-run counts.
- Fixed research-memory pollution in `research_strategy`: only explicit stable-candidate-validation flows can promote candidates to `stable_candidate`, and a reconciliation pass now demotes old false promotions based on current frontier focus. After reconciliation, `mom_20`, `mom_plus_value`, `liquidity_turnover_shock`, `value_ep`, `value_bp`, `size_small`, `hybrid_mom_20_liquidity_turnover_shock`, and `hybrid_liquidity_turnover_shock_value_ep` were pushed back down from stale stable/provisional states, while the current preferred stable set in memory is the three frontier candidates above.
- Reworked the Factor Lab A/B harness to use a frozen `artifacts/ab_harness/input_snapshot.json`, so reruns are reproducible and do not drift with live DB state.
- Fixed multiple A/B harness integrity bugs: batch jobs now clear old output dirs before rerun, harness summaries are deleted before a fresh run, and failed reruns no longer look like successful runs by accidentally reading stale artifacts.
- Fixed Factor Lab data-cache recovery: if the main feature-store parquet is missing/corrupted, the system now restores the newest readable `.corrupt.*` backup instead of collapsing to an empty master; empty incremental pulls also no longer overwrite a good master.
- Fixed Tushare request-cache handling: zero-row cache CSVs are now quarantined instead of treated as valid cache hits, and concurrent cache quarantine/read races no longer crash reruns with `FileNotFoundError`.
- Fixed promotion-scorecard contamination: latest-run selection now excludes A/B harness runs under `artifacts/generated_ab_configs/` and `artifacts/ab_harness/`, so frontier scoring uses official workflow runs instead of single-factor harness runs.
- Added a separate short-window candidate field, `latest_recent_final_score`, and switched frontier ranking / leaderboard-style views to prefer it over `latest_final_score`; this prevents a single long-window official run from unfairly crushing short-window frontier candidates.
- After the reproducible harness and short-window score fix, the main research conclusion is stronger: the frontier/research-selected route beats the fair legacy baseline on neutralized survival and graveyard rate, but neither route has medium-window survivors yet. The edge is real but still short-window/fragile rather than cross-window robust.
- Found and fixed a real `medium_horizon_validation` bug in `research_family_generators`: generated 60d/90d/120d configs were not injecting the target hybrid candidates, so the supposed medium-horizon validation was silently rerunning only the base six factors. Regenerated the medium-horizon configs with candidate expressions included.
- Re-ran the corrected 60d medium-horizon workflow for `hybrid_mom_20_liquidity_turnover_shock`. The result is now trustworthy and negative: the target hybrid itself appears in the run, fails raw (`rank_ic_mean=0.013346`, negative spread) and fails neutralized (negative spread), with zero candidate-pool survivors. This strengthens the current conclusion that the frontier alpha is still short-window and does not survive the 60d medium window.
- Also ran the corrected 60d medium-horizon workflow for `hybrid_mom_20_value_ep`. It fails even more clearly than the liquidity-turnover hybrid: raw `rank_ic_mean=0.001261`, neutralized `rank_ic_mean=-0.017444`, negative spread, split-fail count 2, and zero candidate-pool survivors. Together these corrected reruns strongly suggest the current frontier leaders are recent-window artifacts rather than medium-horizon survivors.
