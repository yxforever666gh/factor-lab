2026-04-08

- 已为 OpenClaw 做本地 durable / 类 Codex 自动续跑补丁。
- 运行时代码改动位于 `/home/admin/.openclaw/openclaw-runtime/dist/` 的 `agent-command-DCy1nGSb.js`、`register.agent-pG6HVfhZ.js`、`zod-schema-C3jh3SvI.js`。
- 实际默认配置已写入 `/home/admin/.openclaw/openclaw.json`，`agents.defaults.executionMode = "durable"`。
- 升级后回放和适配参考文档位于 `/home/admin/.openclaw/LOCAL_CHANGELOG.md`。
- [Wed 2026-04-08 04:40 GMT+8] 用户确认当前研究系统后续推进顺序：先做 regime-aware scorer，再做 budget allocator / bandit；随后由用户盯 live 结果并继续迭代，目标是把系统修到能真正开始稳定长出新 candidate 为止。
- [Wed 2026-04-08 04:54 GMT+8] 已落地第一版 regime-aware opportunity stack：新增 `src/factor_lab/regime_awareness.py` 统一从 frontier/queue/flow/duplicate pressure 提取 regime context；`src/factor_lab/opportunity_scorer.py` 现在按 regime weights 调整 priority/novelty/confidence 并输出 regime 标记；`src/factor_lab/opportunity_policy.py` 的 budget allocator 增加 regime 偏置 + 简单 bandit 分配；`src/factor_lab/exploration_budget.py` 也继承同一套 regime 预算，避免 question generation 仍沿用旧静态逻辑；`src/factor_lab/research_opportunity_engine.py` 输出 opportunity 时带上 regime / regime_confidence。新增回归测试并通过，且用 `artifacts/research_planner_snapshot.json` 做了集成检查，当前快照下预算倾向 `diagnose=6, confirm=5, recombine=3, probe=3, expand=1`，顶层机会已转向 crowded-frontier regime 下的 diagnose 主导。
- [Wed 2026-04-08 04:57 GMT+8] 已把 regime-aware admission 继续下沉到 candidate generation / candidate pool：`src/factor_lab/candidate_generator.py` 现在会基于同一套 regime context 计算 `candidate_floor`、`relaxed_admission`、`generation_anchors`，在 crowded/regime-sensitive frontier 下即使前几个模板对被历史失败冷却挡住，也会继续扫描后续模板并允许受控 retry，不再因为 `stable_candidates` 为空或前两个 pair 被封就直接产出 0 proposal；`src/factor_lab/research_candidate_pool.py` 的 `generated_candidate_budget` 也改为 regime-aware，在 crowded frontier 下可保留 2 个 generated candidate 进入主线。新增 candidate generation 回归测试后，`tests/test_candidate_generation.py` + `tests/test_research_opportunity_autonomy.py` 共 14 项通过。针对真实快照的集成检查显示：candidate generation 已从 0 proposal 变成 2 proposal（`gen__combine_sub__quality_roe__value_ep`、`gen__combine_sub__earnings_yield_over_pb__mom_60_skip_5`），candidate pool 也已给出 2 个对应 generated_candidate 任务，说明系统已开始重新长出新 candidate，而不是停在机会层空转。
- [Wed 2026-04-08 05:38 GMT+8] 继续修 live admission 瓶颈：发现当前真实快照下第二个 proposal 仍被 `max_variants_per_cluster_per_cycle=1` 卡住，因为几乎所有 hypothesis-template pair 都落在同一 cluster；已修改 `src/factor_lab/candidate_generator.py`，当 regime 触发 `relaxed_admission` 且 `candidate_floor > 1` 时，同步放宽单 cluster 每轮可生成变体数到 `candidate_floor`，并把 relaxed retry 从“仅 0 proposal 时兜底”升级为“少于 candidate_floor 就继续补位”。同时修复并发读 `artifacts/research_memory.json` 时的半截 JSON 崩口：`src/factor_lab/candidate_generator.py`、`src/factor_lab/research_family_generators.py`、`src/factor_lab/research_learning.py` 现对 `JSONDecodeError` 做降级容错，避免 live daemon/手动刷新因瞬时半写文件直接炸掉。新增回归后，`tests/test_candidate_generation.py` + `tests/test_research_opportunity_autonomy.py` 共 15 项通过；实际已刷新真实产物 `artifacts/candidate_generation_plan.json` 与 `artifacts/research_candidate_pool.json`，现稳定为 `proposal_count=2`、`generated_candidate_task_count=2`，对应 live 候选为 `gen__residualize_against_peer__earnings_yield_over_pb__mom_60_skip_5` 与 `gen__orthogonalize_against_peer__book_yield__size_small`。之后已重启 `factor-lab-research-daemon.service` 让 live 进程吃到最终修正。
- [Wed 2026-04-08 05:43 GMT+8] 修 UI 健康卡片口径：`src/factor_lab/webui_app.py` 现在将“当前快照分”和“24h 时间线末点”显式分离；研究进步主卡片在时间线没有有效 candidate 轨迹时回退显示 snapshot 分并给出 warning，避免出现稳定候选很多但主值仍固定 20 的误导。组合进步不再读取 `llm_input_snapshot.json` 中陈旧的 `paper_portfolio_stability`，改为优先读取 `artifacts/paper_portfolio/portfolio_stability_score.json` 的 live 值；当前实际从旧显示 `69.5` 修正为 live 快照 `39.1`（Sharpe 14.3 + 稳定性 4.8 + return gate 20.0）。同步更新 `src/factor_lab/webui_templates/dashboard.html` 与 `src/factor_lab/webui_templates/health.html`，把主值来源、snapshot/timeline 对比和分数组成拆解直接展示出来。新增 `tests/test_webui_health_metrics.py`，连同相关测试共 18 项通过。
- [Wed 2026-04-08 06:20 GMT+8] 新增“项目进步观察”量化面板到 `health` 页：在 `src/factor_lab/webui_app.py` 中构建 24h/7d 两种模式的项目进步观察序列，主图使用单条 `项目进步总分`（由 `因子发现质量`、`研究信息增益`、`组合边际` 三项合成），`24h` 采用小时聚合，`7d` 采用按天均值聚合。明确选择“日均值”而不是“日最高值”，原因是最高值会放大单次 spike / 幸运 run，均值更适合判断一整天是否真的在进步。`src/factor_lab/webui_templates/health.html` 增加 24h/7d 切换按钮、当前 24h 任务/结果/高信息占比/低价值重复计数，以及三项分数组成；`src/factor_lab/webui_templates/base.html` 增加 segmented toggle 样式。期间修复 `timezone` import 漏掉导致的 helper 报错，并重启 `factor-lab-web-ui.service`；`/health` 现已 200 正常返回新版内容。相关测试维持 18 项通过。
2026-04-08

- 已为 OpenClaw 做本地 durable / 类 Codex 自动续跑补丁。
- 运行时代码改动位于 `/home/admin/.openclaw/openclaw-runtime/dist/` 的 `agent-command-DCy1nGSb.js`、`register.agent-pG6HVfhZ.js`、`zod-schema-C3jh3SvI.js`。
- 实际默认配置已写入 `/home/admin/.openclaw/openclaw.json`，`agents.defaults.executionMode = "durable"`。
- 升级后回放和适配参考文档位于 `/home/admin/.openclaw/LOCAL_CHANGELOG.md`。
- [Wed 2026-04-08 04:40 GMT+8] 用户确认当前研究系统后续推进顺序：先做 regime-aware scorer，再做 budget allocator / bandit；随后由用户盯 live 结果并继续迭代，目标是把系统修到能真正开始稳定长出新 candidate 为止。
- [Wed 2026-04-08 04:54 GMT+8] 已落地第一版 regime-aware opportunity stack：新增 `src/factor_lab/regime_awareness.py` 统一从 frontier/queue/flow/duplicate pressure 提取 regime context；`src/factor_lab/opportunity_scorer.py` 现在按 regime weights 调整 priority/novelty/confidence 并输出 regime 标记；`src/factor_lab/opportunity_policy.py` 的 budget allocator 增加 regime 偏置 + 简单 bandit 分配；`src/factor_lab/exploration_budget.py` 也继承同一套 regime 预算，避免 question generation 仍沿用旧静态逻辑；`src/factor_lab/research_opportunity_engine.py` 输出 opportunity 时带上 regime / regime_confidence。新增回归测试并通过，且用 `artifacts/research_planner_snapshot.json` 做了集成检查，当前快照下预算倾向 `diagnose=6, confirm=5, recombine=3, probe=3, expand=1`，顶层机会已转向 crowded-frontier regime 下的 diagnose 主导。
- [Wed 2026-04-08 04:57 GMT+8] 已把 regime-aware admission 继续下沉到 candidate generation / candidate pool：`src/factor_lab/candidate_generator.py` 现在会基于同一套 regime context 计算 `candidate_floor`、`relaxed_admission`、`generation_anchors`，在 crowded/regime-sensitive frontier 下即使前几个模板对被历史失败冷却挡住，也会继续扫描后续模板并允许受控 retry，不再因为 `stable_candidates` 为空或前两个 pair 被封就直接产出 0 proposal；`src/factor_lab/research_candidate_pool.py` 的 `generated_candidate_budget` 也改为 regime-aware，在 crowded frontier 下可保留 2 个 generated candidate 进入主线。新增 candidate generation 回归测试后，`tests/test_candidate_generation.py` + `tests/test_research_opportunity_autonomy.py` 共 14 项通过。针对真实快照的集成检查显示：candidate generation 已从 0 proposal 变成 2 proposal（`gen__combine_sub__quality_roe__value_ep`、`gen__combine_sub__earnings_yield_over_pb__mom_60_skip_5`），candidate pool 也已给出 2 个对应 generated_candidate 任务，说明系统已开始重新长出新 candidate，而不是停在机会层空转。
- [Wed 2026-04-08 05:38 GMT+8] 继续修 live admission 瓶颈：发现当前真实快照下第二个 proposal 仍被 `max_variants_per_cluster_per_cycle=1` 卡住，因为几乎所有 hypothesis-template pair 都落在同一 cluster；已修改 `src/factor_lab/candidate_generator.py`，当 regime 触发 `relaxed_admission` 且 `candidate_floor > 1` 时，同步放宽单 cluster 每轮可生成变体数到 `candidate_floor`，并把 relaxed retry 从“仅 0 proposal 时兜底”升级为“少于 candidate_floor 就继续补位”。同时修复并发读 `artifacts/research_memory.json` 时的半截 JSON 崩口：`src/factor_lab/candidate_generator.py`、`src/factor_lab/research_family_generators.py`、`src/factor_lab/research_learning.py` 现对 `JSONDecodeError` 做降级容错，避免 live daemon/手动刷新因瞬时半写文件直接炸掉。新增回归后，`tests/test_candidate_generation.py` + `tests/test_research_opportunity_autonomy.py` 共 15 项通过；实际已刷新真实产物 `artifacts/candidate_generation_plan.json` 与 `artifacts/research_candidate_pool.json`，现稳定为 `proposal_count=2`、`generated_candidate_task_count=2`，对应 live 候选为 `gen__residualize_against_peer__earnings_yield_over_pb__mom_60_skip_5` 与 `gen__orthogonalize_against_peer__book_yield__size_small`。之后已重启 `factor-lab-research-daemon.service` 让 live 进程吃到最终修正。
- [Wed 2026-04-08 05:43 GMT+8] 修 UI 健康卡片口径：`src/factor_lab/webui_app.py` 现在将“当前快照分”和“24h 时间线末点”显式分离；研究进步主卡片在时间线没有有效 candidate 轨迹时回退显示 snapshot 分并给出 warning，避免出现稳定候选很多但主值仍固定 20 的误导。组合进步不再读取 `llm_input_snapshot.json` 中陈旧的 `paper_portfolio_stability`，改为优先读取 `artifacts/paper_portfolio/portfolio_stability_score.json` 的 live 值；当前实际从旧显示 `69.5` 修正为 live 快照 `39.1`（Sharpe 14.3 + 稳定性 4.8 + return gate 20.0）。同步更新 `src/factor_lab/webui_templates/dashboard.html` 与 `src/factor_lab/webui_templates/health.html`，把主值来源、snapshot/timeline 对比和分数组成拆解直接展示出来。新增 `tests/test_webui_health_metrics.py`，连同相关测试共 18 项通过。
- [Wed 2026-04-08 05:50 GMT+8] 用户反馈 `127.0.0.1:8765` 首页出现 `Internal Server Error`。原因是 web UI 进程仍跑旧模板/旧上下文结构，模板读取新增 `breakdown` 字段时直接 500。已将模板改成向后兼容并重启 `factor-lab-web-ui.service`；之后首页 `/` 已恢复 200，当前主卡片显示为运行健康 `100.0`、研究进步 `80.0`、组合进步 `39.1`。
- [Wed 2026-04-08 05:57 GMT+8] 对“优化已完成后是否继续改”给出运行策略建议：不建议立刻继续大改，先观察 live；最小观察窗口 4-6 小时，推荐观察窗口 24 小时，高置信窗口 24-48 小时。观察重点是 generated candidate 是否维持 >0、是否继续写回 `research_memory.json`、daemon 是否再次触发 quarantine/rss 问题、candidate pool 是否稳定按 budget 注入，以及 outcome 是否继续带来新 branch 而不是回到低价值重复。
- [Wed 2026-04-08 06:20 GMT+8] 新增“项目进步观察”量化面板到 `health` 页：在 `src/factor_lab/webui_app.py` 中构建 24h/7d 两种模式的项目进步观察序列，主图使用单条 `项目进步总分`（由 `因子发现质量`、`研究信息增益`、`组合边际` 三项合成），`24h` 采用小时聚合，`7d` 采用按天均值聚合。明确选择“日均值”而不是“日最高值”，原因是最高值会放大单次 spike / 幸运 run，均值更适合判断一整天是否真的在进步。`src/factor_lab/webui_templates/health.html` 增加 24h/7d 切换按钮、当前 24h 任务/结果/高信息占比/低价值重复计数，以及三项分数组成；`src/factor_lab/webui_templates/base.html` 增加 segmented toggle 样式。期间修复 `timezone` import 漏掉导致的 helper 报错，并重启 `factor-lab-web-ui.service`；`/health` 现已 200 正常返回新版内容。相关测试维持 18 项通过。
- [Wed 2026-04-08 23:45 GMT+8] 开始执行“修 CPU / 执行链”计划的 P0：针对 `generated candidate validation -> code -15 / rss exceeded -> quarantine -> CPU 掉下去` 这条链，修改 `src/factor_lab/research_queue.py` 与 `src/factor_lab/research_expansion.py`。具体包括：1) `recent_failure_stats()` 现在忽略 `stale_running_task_cleaned` / `stale_running_task_repaired_after_outputs_written` 这类 maintenance failure，避免误触发 circuit；2) orchestrator 对 `candidate_generation_validation` 类 workflow 强制串行执行，不再和其他重 workflow 并发；3) `research_expansion._candidate_validation_specs()` 对 generated candidate revalidation 默认关闭（需显式环境变量 `RESEARCH_ENABLE_GENERATED_CANDIDATE_REVALIDATION=1` 才开启），先切断 generic top-promising 路径里反复生成的坏 revalidation 环；4) 若开启 generated revalidation，则默认只生成 light 45d、缩小 `universe_limit` 到 40、将 rolling window/step 降到 21/7；5) generated candidate validation config 现在会把生成表达式直接编译成 raw-field 可执行表达式，不再带 unresolved lineage 名称；6) 45d light 成功后才排 90d followup。新增 `tests/test_generated_candidate_validation_flow.py`，并与 `tests/test_research_queue_fail_safes.py`、`tests/test_candidate_generation.py`、`tests/test_research_opportunity_autonomy.py` 一起共 29 项通过。live 侧已手动收掉旧的 pending generated revalidation retry（`generated_candidate_revalidation_disabled`），并重启 daemon；重启后 heartbeat 显示它跳出旧坏循环，改走 forced expansion 注入 baseline 任务，随后 `artifacts/research_daemon_status.json` 中 `cpu_usage_ratio` 已升到 `0.9688`、`mode=cpu_saturated`、`processed_count=2`，说明 CPU 和执行吞吐都已明显恢复。后续仍需继续观察 generated candidate 初始 workflow 本身的稳定性，但最直接导致 CPU 掉下去的 revalidation 坏链已经被切断。
