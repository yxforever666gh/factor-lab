- 2026-04-01 early morning: Diagnosed repeated Factor Lab stalls as a combination of stale `running` research tasks, duplicate `run_research_daemon.py` processes, and config-generation bugs.
- Fixed LLM/batch config chain so `factor_family_config` is materialized into explicit `factors` in `scripts/generate_batch_from_llm_plan.py`, `src/factor_lab/proposal_to_batch.py`, and `src/factor_lab/llm_agent.py`; validated end-to-end LLM cycle, batch generation, and execution.
- Added regression coverage for the above in `tests/test_llm_plan_pipeline.py` and related tests passed.
- Implemented daemon single-instance locking in `scripts/run_research_daemon.py` using `artifacts/research_daemon.lock` to stop duplicate daemon processes from corrupting queue state.
- Strengthened auto-recovery in research orchestration so stale/unfinalized `running` tasks are cleaned more aggressively; system resumed autonomous forced expansion instead of waiting for manual reseeding.
- Diagnosed planner `Extra data` failure as a corrupted `artifacts/research_opportunity_store.json` with trailing junk; repaired the file, added tolerant read handling in `src/factor_lab/opportunity_policy.py`, and added tests in `tests/test_opportunity_policy.py`.
- Remaining open issue at flush time: generated opportunity configs under `artifacts/generated_opportunity_configs/` can still preserve relative `factor_family_config` instead of materialized `factors`, causing `missing_factor_family_config` skips; next step is to patch that generation path too.
