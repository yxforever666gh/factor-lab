- Built Factor Lab beyond the original v1.0 baseline into an operational v2.2-alpha style runtime: added configurable web UI on 127.0.0.1:8765, health/weekly/research queue pages, heartbeat logging, daemon status display, and multiple git commits documenting/implementing the new architecture.
- Reworked execution model from cron-driven periodic scripts to a daemon-based autonomous research runtime: created `scripts/run_research_daemon.py`, a user systemd service `factor-lab-research-daemon.service`, and install script; main research execution now runs via daemon + systemd while cron was downgraded to optional/auxiliary checks.
- Added SQLite-backed research task queue and orchestrator flow: `research_tasks` table, queue seeding, queue UI, task types (`workflow`, `batch`, `generated_batch`, `diagnostic`), follow-up generation, retry tasks, knowledge-gain tagging, and guardrails such as empty-queue reseed, repeat suppression, exploration throttling, and circuit-open pausing.
- Identified and fixed several orchestration/runtime issues during live debugging: web UI background process terminations from foreground exec sessions, false cron errors caused by `announce` delivery with no channel, repeated empty-loop reseeding/duplicate task churn, and multiple guardrail/expansion bugs (`timedelta`, cooldown state/constant wiring, missing `maybe_expand_research_space` import, syntax breakage while widening expansion configs).
- Confirmed current stable direction: daemon is running under systemd, no longer rapidly empty-looping, and the research expansion engine now successfully generates and executes broader historical window tasks such as rolling 30d/45d/90d, then expanded further to 60d/120d plus recent/expanding variants; latest observed state before compaction was healthy running/idle after finishing generated window workflows.
- User preference/strategy: prioritize stability over throughput/CPU utilization; user explicitly wants a continuously advancing historical backtest research system, not paper/live trading yet, and wants agent-guided autonomous expansion of research space rather than mere rule-only execution or idle waiting.
